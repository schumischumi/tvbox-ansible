- hosts: localhost
  become: true

  vars_prompt:
    - name: ansible_user
      prompt: "Which is the default user?"
      private: false

  roles:
    - packages
    - flatpak
    - sway
    - updates
