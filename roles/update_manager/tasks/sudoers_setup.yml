- name: Create sudoers.d file for passwordless apt-get update/upgrade
  ansible.builtin.copy:
    content: |
      {{ tv_user }} ALL = NOPASSWD: /usr/bin/apt-get upgrade *
      {{ tv_user }} ALL = NOPASSWD: /usr/bin/apt-get update *
    dest: /etc/sudoers.d/90-apt-upgrade
    owner: root
    group: root
    mode: '0440'
    validate: '/usr/sbin/visudo -cf %s'
