- name: Copy lock rules
  ansible.builtin.copy:
    src: kscreenlockerrc
    dest: "/home/{{ tv_user }}/.config/kscreenlockerrc"
    mode: '0644'
    owner: "{{ tv_user }}"
    group: "{{ tv_user }}"
