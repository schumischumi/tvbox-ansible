- name: Create sway config directory
  ansible.builtin.file:
    path: "/home/{{ tv_user }}/.config/sway/config.d"
    state: directory
    mode: '0755'
    owner: "{{ tv_user }}"
    group: "{{ tv_user }}"

- name: Install sway config
  ansible.builtin.copy:
    src: sway/config
    dest: "/home/{{ tv_user }}/.config/sway/config"
    mode: '0644'
    owner: "{{ tv_user }}"
    group: "{{ tv_user }}"

- name: Install sway autostart config
  ansible.builtin.copy:
    src: sway/autostart.conf
    dest: "/home/{{ tv_user }}/.config/sway/config.d/autostart.conf"
    mode: '0644'
    owner: "{{ tv_user }}"
    group: "{{ tv_user }}"
